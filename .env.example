# 複製這個檔案為 .env 並填入你的金鑰：
#   cp .env.example .env
#
# .env 已被 .gitignore 排除，不會外洩。
# 此檔僅供本機開發使用；在 Replit 上請改用 Secrets 設定。

# ─── API server ──────────────────────────────────────────────────────────────
# 必填。沒有設定時 api-server 會直接拋錯結束。
PORT=5000

NODE_ENV=development

# 必填。缺少時 POST /analyze 會回傳 500。
# 取得：https://aistudio.google.com/
GEMINI_API_KEY=

# 選填備援金鑰。伺服器會遍歷「模型 × 金鑰」所有組合，任一成功即採用，
# 因此第一把額度用完時會自動改用下一把。最多支援三把。
GEMINI_API_KEY_2=
GEMINI_API_KEY_3=

# 選填。沒有的話仍可分析，只是不會帶入近期新聞。
# 取得：https://tavily.com/
TAVILY_API_KEY=

# 選填。沒有 token 仍可查詢，但速率限制較嚴格。
# 取得：https://finmindtrade.com/
FINMIND_TOKEN=

# ─── Mobile ──────────────────────────────────────────────────────────────────
# 手機 App 要呼叫的後端位址。
#
# 這個變數要放在 artifacts/mobile/.env（Expo 只讀該套件目錄下的 .env），
# 而且只有在預設值不適用時才需要設定。
#
# 預設值 http://localhost:5000 適用於：網頁版（按 w）與 Android 模擬器。
# 用實體手機搭配 Expo Go 時，localhost 指的是手機自己，連不到你的電腦，
# 必須改成電腦的區網 IP，例如：
#   EXPO_PUBLIC_API_URL=http://192.168.1.23:5000
#
# 查自己的區網 IP（PowerShell）：
#   (Get-NetIPAddress -AddressFamily IPv4 -PrefixOrigin Dhcp).IPAddress
